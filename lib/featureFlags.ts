/**
 * Flag-uri pentru funcționalități ce pot fi dezactivate temporar.
 * Setează la true pentru a reporni validările.
 */

/** Validări care cer cel puțin o imagine per tăviță: recepție (trimitere), departamente (finalizare), tehnician. */
export const MANDATORY_TRAY_IMAGES_ENABLED = false
